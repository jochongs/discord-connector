import axios from "axios";
import { GuildService } from "./level-1/guild/guild.service";

async function main() {
  console.log(process.env.BOT_TOKEN);
  //const guildService = new GuildService(axios);
}

main();
